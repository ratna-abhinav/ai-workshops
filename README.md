# ai-workshops

Collection of AI workshops. Each folder is a self-contained workshop
with its own environment and setup instructions.

- `vivek-ai-workshop/` — Agentic AI workshop (LangGraph, MLflow Gateway,
  Judge0). Start here:

```bash
cd vivek-ai-workshop
uv sync
```

Then follow `vivek-ai-workshop/README.md`.
